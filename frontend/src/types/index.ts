export type SalesData = {
    id: number;
    vendorId: string;
    customerId: string;
    ticketId: string;
    timestamp: string;
  };

export type FieldType = {
    username?: string;
    password?: string;
    remember?: string;
  };